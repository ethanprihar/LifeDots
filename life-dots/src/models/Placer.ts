export default class Placer
{
    init(rows: number, cols: number): Record<string, any>
    {
        return {};
    }

    update(map: Record<string, any>): void {}

    brush(map: Record<string, any>, row: number, col: number): void {}
}